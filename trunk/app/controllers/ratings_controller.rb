class RatingsController < ApplicationController

  #def rate
   # @rating = Rating.find(params[:id])
   # Rating.delete_all(["rateable_type = 'Rating' AND rateable_id = ? AND user_id = ?",
   #   @rating.id, current_user.id])
   #  @rating.add_rating Rating.new(:rating => params[:rating],
  #    :user_id => current_user.id)
  #end
end
